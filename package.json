{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/bleh-ui",
    "packages/bleh-ui-icons",
    "apps/docs"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun run build-packages && bun run build-docs",
    "build-packages": "bun --filter './packages/*' build",
    "build-docs": "bun --filter './apps/docs' build"
  }
}
