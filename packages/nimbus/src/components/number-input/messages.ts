import { defineMessages } from "react-intl";

export default defineMessages({
  decrement: {
    id: "Nimbus.numberInput.decrement",
    description: "aria-label & tooltip for decrement button in number input",
    defaultMessage: "Decrement",
  },
  increment: {
    id: "Nimbus.numberInput.increment",
    description: "aria-label & tooltip for increment button in number input",
    defaultMessage: "Increment",
  },
});
