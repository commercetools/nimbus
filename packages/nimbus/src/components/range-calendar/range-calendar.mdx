---
id: Components-RangeCalendar
title: RangeCalendar
description: Filler for now, not sure you're going to get a designated file. 
documentState: InitialDraft
order: 999
menu:
  - Components
  - Inputs
  - RangeCalendar
tags:
  - component
---

# RangeCalendar

Filler for now, not sure you're going to get a designated file. 

## Basic usage

[Explain the basic usage / usecase of the component].

```jsx-live
const App = () => <RangeCalendar/>
```
{/* 

### Sizes

Available sizes.

```jsx-live
const App = () => {

  const sizes = ['2xl', 'xl', 'lg', 'md', 'sm', 'xs', '2xs'];

  return (
    <Stack direction="horizontal" alignItems="center">
      {sizes.map(size => (
        <RangeCalendar key={size} size={size}>'{size}' RangeCalendar</RangeCalendar>
      ))}
    </Stack>
  )
}
```

### Variants

Available variants.

```jsx-live
const App = () => {

  const variants = ['solid', 'subtle', 'outline', 'ghost', 'plain'];

  return (
    <Stack direction="horizontal">
      {variants.map(variant => (
        <RangeCalendar key={variant} variant={variant}>'{variant}' RangeCalendar</RangeCalendar>
      ))}
    </Stack>
  )
}
```

### Colors

[Explain usage with different colors/semantics.]

```jsx-live
const App = () => {
  const variants = ["solid", "subtle", "outline", "ghost", "link", "plain"];
  const colors = ["neutral", "primary", "info", "positive", "warning", "critical"];

  return (
    <Stack>
      {colors.map((color) => (
        <Stack key={color} direction="horizontal">
          {variants.map((variant) => (
            <RangeCalendar colorPalette={color} key={variant} variant={variant}>
              '{variant}' RangeCalendar
            </RangeCalendar>
          ))}
        </Stack>
      ))}
    </Stack>
  );
};
``` */}

## Properties

<GroupedPropsTable componentName="RangeCalendar" />
