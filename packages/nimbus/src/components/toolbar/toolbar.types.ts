import type {
  ToolbarRootSlotProps,
  ToolbarGroupSlotProps,
  ToolbarSeparatorSlotProps,
} from "./toolbar.slots.tsx";

export interface ToolbarRootProps extends ToolbarRootSlotProps {}
export interface ToolbarGroupProps extends ToolbarGroupSlotProps {}
export interface ToolbarSeparatorProps extends ToolbarSeparatorSlotProps {}
