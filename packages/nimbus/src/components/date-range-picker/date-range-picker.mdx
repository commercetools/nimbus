---
id: Components-DateRangePicker
title: DateRangePicker
description: Allows users to select a start and end date from a calendar interface.
documentState: InitialDraft
order: 999
menu:
  - Components
  - Inputs
  - DateRangePicker
tags:
  - component
---

# DateRangePicker

Allows users to select a start and end date from a calendar interface.

## Basic usage

[Explain the basic usage / usecase of the component].

```jsx-live
const App = () => <DateRangePicker>I am DateRangePicker!</DateRangePicker>
```
{/* 

### Sizes

Available sizes.

```jsx-live
const App = () => {

  const sizes = ['2xl', 'xl', 'lg', 'md', 'sm', 'xs', '2xs'];

  return (
    <Stack direction="horizontal" alignItems="center">
      {sizes.map(size => (
        <DateRangePicker key={size} size={size}>'{size}' DateRangePicker</DateRangePicker>
      ))}
    </Stack>
  )
}
```

### Variants

Available variants.

```jsx-live
const App = () => {

  const variants = ['solid', 'subtle', 'outline', 'ghost', 'plain'];

  return (
    <Stack direction="horizontal">
      {variants.map(variant => (
        <DateRangePicker key={variant} variant={variant}>'{variant}' DateRangePicker</DateRangePicker>
      ))}
    </Stack>
  )
}
```

### Colors

[Explain usage with different colors/semantics.]

```jsx-live
const App = () => {
  const variants = ["solid", "subtle", "outline", "ghost", "link", "plain"];
  const colors = ["neutral", "primary", "info", "positive", "warning", "critical"];

  return (
    <Stack>
      {colors.map((color) => (
        <Stack key={color} direction="horizontal">
          {variants.map((variant) => (
            <DateRangePicker colorPalette={color} key={variant} variant={variant}>
              '{variant}' DateRangePicker
            </DateRangePicker>
          ))}
        </Stack>
      ))}
    </Stack>
  );
};
``` */}

## Props

<PropsTable id="DateRangePicker" />
