// Re-export the shared modal recipe with Dialog-specific defaults and alias
export { modalSlotRecipe as dialogSlotRecipe } from "../modal/modal.recipe";

/**
 * @deprecated Use modalSlotRecipe from Modal component instead.
 * This alias is maintained for backward compatibility.
 */
