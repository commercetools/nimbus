import type { IconButtonProps } from "../icon-button/icon-button.types";

// ============================================================
// MAIN PROPS
// ============================================================

/**
 * The FloatingActionButton adds no API of its own — it is a circular, elevated
 * {@link IconButton} with FAB styling defaults. This type mirrors
 * {@link IconButtonProps} and exists only for naming consistency with other
 * components.
 */
export type FloatingActionButtonProps = IconButtonProps;
