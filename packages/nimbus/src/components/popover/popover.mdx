---
id: Components-Popover
title: Popover
description: An overlay element positioned relative to a trigger.
documentState: InitialDraft
order: 999
menu:
  - Components
  - Popover
tags:
  - component
---

# Popover

An overlay element positioned relative to a trigger.

## Basic Usage

[Explain the basic usage / usecase of the component].

```jsx-live
const App = () => <Popover>I am Popover!</Popover>
```


### Sizes

Available sizes.

```jsx-live
const App = () => {

  const sizes = ['2xl', 'xl', 'lg', 'md', 'sm', 'xs', '2xs'];

  return (
    <Stack direction="horizontal" alignItems="center">
      {sizes.map(size => (
        <Popover key={size} size={size}>'{size}' Popover</Popover>
      ))}
    </Stack>
  )
}
```

### Variants

Available variants.

```jsx-live
const App = () => {

  const variants = ['solid', 'subtle', 'outline', 'ghost', 'plain'];

  return (
    <Stack direction="horizontal">
      {variants.map(variant => (
        <Popover key={variant} variant={variant}>'{variant}' Popover</Popover>
      ))}
    </Stack>
  )
}
```

### Colors

[Explain usage with different colors/semantics.]

```jsx-live
const App = () => {
  const variants = ["solid", "subtle", "outline", "ghost", "link", "plain"];
  const colors = ["neutral", "primary", "info", "positive", "warning", "critical"];

  return (
    <Stack>
      {colors.map((color) => (
        <Stack key={color} direction="horizontal">
          {variants.map((variant) => (
            <Popover colorPalette={color} key={variant} variant={variant}>
              '{variant}' Popover
            </Popover>
          ))}
        </Stack>
      ))}
    </Stack>
  );
};
```

## Props

<PropsTable id="Popover" />
