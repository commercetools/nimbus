/**
 * Re-exports of React Aria's Virtualizer prop types and layout option types.
 *
 * These types are provided as a convenience so consumers can type their layout
 * options and virtualizer props without adding a direct dependency on
 * `react-aria-components` or `react-stately`.
 */

export type { VirtualizerProps } from "react-aria-components/Virtualizer";

export type {
  ListLayoutOptions,
  GridLayoutOptions,
  WaterfallLayoutOptions,
  TableLayoutProps,
} from "react-aria-components/Virtualizer";
