---
id: Components-FormField
title: FormField
description: displays inputs in a FormField context
documentState: InitialDraft
order: 999
menu:
  - Components
  - FormField
tags:
  - component
---

# FormField

displays inputs in a FormField context

## Basic Usage

[Explain the basic usage / usecase of the component].

```jsx-live
const App = () => <FormField>I am FormField!</FormField>
```


### Sizes

Available sizes.

```jsx-live
const App = () => {

  const sizes = ['2xl', 'xl', 'lg', 'md', 'sm', 'xs', '2xs'];

  return (
    <Stack direction="horizontal" alignItems="center">
      {sizes.map(size => (
        <FormField key={size} size={size}>'{size}' FormField</FormField>
      ))}
    </Stack>
  )
}
```

### Variants

Available variants.

```jsx-live
const App = () => {

  const variants = ['solid', 'subtle', 'outline', 'ghost', 'plain'];

  return (
    <Stack direction="horizontal">
      {variants.map(variant => (
        <FormField key={variant} variant={variant}>'{variant}' FormField</FormField>
      ))}
    </Stack>
  )
}
```

### Colors

[Explain usage with different colors/semantics.]

```jsx-live
const App = () => {
  const variants = ["solid", "subtle", "outline", "ghost", "link", "plain"];
  const colors = ["neutral", "primary", "info", "success", "danger", "error"];

  return (
    <Stack>
      {colors.map((color) => (
        <Stack key={color} direction="horizontal">
          {variants.map((variant) => (
            <FormField colorPalette={color} key={variant} variant={variant}>
              '{variant}' FormField
            </FormField>
          ))}
        </Stack>
      ))}
    </Stack>
  );
};
```

## Props

<PropTable id="FormField" />