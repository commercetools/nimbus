---
id: Components-IconButton
title: IconButton
description: displays a button with only an icon as child
documentState: InitialDraft
order: 999
menu:
  - Components
  - IconButton
tags:
  - component
---

# IconButton

displays a button with only an icon as child

## Basic Usage

[Explain the basic usage / usecase of the component].

```jsx-live
const App = () => <IconButton>I am IconButton!</IconButton>
```


### Sizes

Available sizes.

```jsx-live
const App = () => {

  const sizes = ['2xl', 'xl', 'lg', 'md', 'sm', 'xs', '2xs'];

  return (
    <Stack direction="horizontal" alignItems="center">
      {sizes.map(size => (
        <IconButton key={size} size={size}>'{size}' IconButton</IconButton>
      ))}
    </Stack>
  )
}
```

### Variants

Available variants.

```jsx-live
const App = () => {

  const variants = ['solid', 'subtle', 'outline', 'ghost', 'plain'];

  return (
    <Stack direction="horizontal">
      {variants.map(variant => (
        <IconButton key={variant} variant={variant}>'{variant}' IconButton</IconButton>
      ))}
    </Stack>
  )
}
```

### Colors

[Explain usage with different colors/semantics.]

```jsx-live
const App = () => {
  const variants = ["solid", "subtle", "outline", "ghost", "link", "plain"];
  const colors = ["neutral", "primary", "info", "success", "danger", "error"];

  return (
    <Stack>
      {colors.map((color) => (
        <Stack key={color} direction="horizontal">
          {variants.map((variant) => (
            <IconButton colorPalette={color} key={variant} variant={variant}>
              '{variant}' IconButton
            </IconButton>
          ))}
        </Stack>
      ))}
    </Stack>
  );
};
```

## Props

<PropTable id="IconButton" />