---
id: Components-MehButton
title: MehButton
description: display a boring button
documentState: InitialDraft
order: 999
menu:
  - Components
  - MehButton
tags:
  - component
---

# MehButton

display a boring button

## Basic Usage

[Explain the basic usage / usecase of the component].

```jsx-live
const App = () => <MehButton>I am MehButton!</MehButton>
```


### Sizes

Available sizes.

```jsx-live
const App = () => {

  const sizes = ['2xl', 'xl', 'lg', 'md', 'sm', 'xs', '2xs'];

  return (
    <Stack direction="horizontal" alignItems="center">
      {sizes.map(size => (
        <MehButton key={size} size={size}>'{size}' MehButton</MehButton>
      ))}
    </Stack>
  )
}
```

### Variants

Available variants.

```jsx-live
const App = () => {

  const variants = ['solid', 'subtle', 'outline', 'ghost', 'plain'];

  return (
    <Stack direction="horizontal">
      {variants.map(variant => (
        <MehButton key={variant} variant={variant}>'{variant}' MehButton</MehButton>
      ))}
    </Stack>
  )
}
```

### Colors

[Explain usage with different colors/semantics.]

```jsx-live
const App = () => {
  const variants = ["solid", "subtle", "outline", "ghost", "link", "plain"];
  const colors = ["neutral", "primary", "info", "success", "danger", "error"];

  return (
    <Stack>
      {colors.map((color) => (
        <Stack key={color} direction="horizontal">
          {variants.map((variant) => (
            <MehButton colorPalette={color} key={variant} variant={variant}>
              '{variant}' MehButton
            </MehButton>
          ))}
        </Stack>
      ))}
    </Stack>
  );
};
```

## Props

<PropTable id="MehButton" />