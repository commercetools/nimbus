---
id: Components-Checkbox
title: Checkbox
description: displays a checkbox and an associated label
documentState: InitialDraft
order: 999
menu:
  - Components
  - Inputs
  - Checkbox
tags:
  - component
---

# Checkbox

displays a checkbox and an associated label

## With label

A checkbox with a visible label.

```jsx-live
const App = () => <Checkbox size="md">I am a Checkbox</Checkbox>
```

## Without visible label

A checkbox without a visible label.

```jsx-live
const App = () => <Checkbox size="md" aria-label="I am a Checkbox"/>
```

## Controlled Checkbox

A checkbox that is controlled by a state.

```jsx-live
const App = () => {

  const [isSelected, setIsSelected]  = useState(true)

  return (
   <Checkbox size="md" isSelected={isSelected} onChange={(v)=>setIsSelected(v)}>
     Controlled Checkbox
   </Checkbox>
  )
}
```

## Disabled Checkbox

A checkbox that is disabled.

```jsx-live
const App = () => <Checkbox size="md" isDisabled>Disabled Checkbox</Checkbox>
```

## Invalid Checkbox

A checkbox that is marked as invalid.

```jsx-live
const App = () => <Checkbox size="md" isInvalid>Invalid Checkbox</Checkbox>
```

## Indeterminate Checkbox

A checkbox that is in an indeterminate state.

```jsx-live
const App = () => <Checkbox size="md" isIndeterminate>Indeterminate Checkbox</Checkbox>
```

## Props

<PropTable id="Checkbox" />
