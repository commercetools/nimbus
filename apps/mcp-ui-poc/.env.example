# Environment variables for MCP UI POC applications
# Copy this file to .env and fill in your actual values

# ============================================================================
# CLIENT BUILD CONFIGURATION
# ============================================================================

# Anthropic API Key (required for client build)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# UI MCP Server URL (required for client build)
# This is the URL the browser will use to connect to the UI MCP server
# For local development: http://localhost:3001
# For production/GKE: http://YOUR_UI_SERVER_LOAD_BALANCER_IP:80
UI_MCP_SERVER_URL=http://localhost:3001

# Commerce MCP Server URL (optional for client build)
# This is the URL the browser will use to connect to the Commerce MCP server
# For local development: http://localhost:8888
# For production/GKE: http://YOUR_COMMERCE_SERVER_LOAD_BALANCER_IP:80
# Leave empty if commerce server is not deployed
COMMERCE_MCP_SERVER_URL=

# ============================================================================
# COMMERCETOOLS CONFIGURATION (for commerce-mcp-server)
# ============================================================================
# These credentials are used by the commerce MCP server to access commercetools API
# They are injected into Kubernetes as secrets, not baked into Docker images

CT_PROJECT_KEY=your-project-key
CT_CLIENT_ID=your-client-id
CT_CLIENT_SECRET=your-client-secret
CT_AUTH_URL=https://auth.europe-west1.gcp.commercetools.com
CT_API_URL=https://api.europe-west1.gcp.commercetools.com

# ============================================================================
# DOCKER REGISTRY CONFIGURATION (optional)
# ============================================================================
# Docker registry for pushing images
# Default: us-east1-docker.pkg.dev/ctp-playground/byron-wall
# REGISTRY=us-east1-docker.pkg.dev/ctp-playground/byron-wall
