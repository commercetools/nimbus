---
"@commercetools/nimbus": patch
---

Fix LocalizedField to use `md` as the default size variant, aligning with
FormField and other components. Also fixes inputs not stretching to fill
container width.
