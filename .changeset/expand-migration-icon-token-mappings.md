---
"@commercetools/nimbus-mcp": minor
---

`migrate_from_uikit`: Add 142 specific icon migration mappings (e.g.
`BinFilledIcon` → `SvgDelete`), direct mappings for `SearchTextInput` and
`ViewSwitcher`, and token redirect for `customProperties`/`designTokens`
imports.
