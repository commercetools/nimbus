---
"@commercetools/nimbus": minor
---

**Slider / RangeSlider**: add a `variant` prop with two visual treatments —
`plain` (default, a thin filled track with a knob) and `enclosed` (a thick,
contained iOS-style bar with the knob inset inside it). Applies to both `Slider`
and `RangeSlider`.

Also fixes the thumb rendering slightly off-center on the track — it now centers
correctly on the cross-axis in both horizontal and vertical orientations.
