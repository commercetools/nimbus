---
"@commercetools/nimbus": patch
---

`DataTable`: frozen (sticky) columns — including the selection/checkbox column —
now follow the row's hover and selection highlight instead of keeping their own
background, so the highlight reads as one continuous row across the frozen
columns.
